#include <stdio.h>
/**
 * main - this functions output is lowercase alphabet
 *
 * Return: 0 on success
 */
int main(void)
{
for (char low_alpha = 'a' ; low_alpha <= 'z' ; low_alpha++)
putchar(low_alpha);
}
putchar('\n');
return (0);
}
